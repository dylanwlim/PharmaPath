name: Vercel deployment sync check

on:
  push:
    branches:
      - main

permissions: {}

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Satisfy Vercel sync deployment check
        run: echo "sync check satisfied for Vercel production gating."
